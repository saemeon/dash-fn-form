{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"dash-fn-form","text":"<p>Headless engine for <code>dash-interact</code>. Introspects a type-hinted Python callable and builds a reactive Dash form panel.</p> <p>Most users should install <code>dash-interact</code> instead \u2014 it includes this package and adds the <code>page</code> API.</p>"},{"location":"#installation","title":"Installation","text":"<pre><code>pip install dash-fn-form\n</code></pre>"},{"location":"#usage","title":"Usage","text":"<pre><code>from dash import Dash, html\nfrom dash_fn_form import build_fn_panel\n\napp = Dash(__name__)\n\ndef sine_wave(amplitude: float = 1.0, frequency: float = 2.0):\n    import numpy as np, plotly.graph_objects as go\n    x = np.linspace(0, 6 * np.pi, 600)\n    return go.Figure(go.Scatter(x=x, y=amplitude * np.sin(frequency * x)))\n\npanel = build_fn_panel(sine_wave)\n\napp.layout = html.Div([panel])\napp.run(debug=True)\n</code></pre> <p><code>build_fn_panel</code> returns an <code>html.Div</code> containing the form and output area \u2014 wire it into any layout.</p>"},{"location":"#api","title":"API","text":""},{"location":"#build_fn_panel","title":"build_fn_panel","text":"<p>Build and return a self-contained interactive panel.</p> <p>Registers Dash callbacks and returns an <code>html.Div</code>.  Has no knowledge of pages; the caller is responsible for placing the panel in a layout.</p> PARAMETER DESCRIPTION <code>fn</code> <p>Callable whose parameters define the form fields.</p> <p> TYPE: <code>Callable</code> </p> <code>_id</code> <p>Explicit component-ID namespace.  Defaults to <code>fn.__name__</code>. Pass a unique string when two panels wrap functions with the same name to prevent Dash component-ID collisions.</p> <p> TYPE: <code>str | None</code> DEFAULT: <code>None</code> </p> <code>_manual</code> <p><code>False</code> \u2014 live update on every field change. <code>True</code> \u2014 Apply button; callback fires on click only.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>_loading</code> <p>Wrap the output area in <code>dcc.Loading</code> (default <code>True</code>).</p> <p> TYPE: <code>bool</code> DEFAULT: <code>True</code> </p> <code>_render</code> <p>Optional converter applied to fn's return value before display.</p> <p> TYPE: <code>Callable[[Any], Any] | None</code> DEFAULT: <code>None</code> </p> <code>_cache</code> <p>Cache function call results by input values (default <code>False</code>). Skips re-calling fn when the same field values are submitted again.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> <code>_cache_maxsize</code> <p>Maximum number of cached results (LRU eviction).  Default <code>128</code>.</p> <p> TYPE: <code>int</code> DEFAULT: <code>128</code> </p> <code>**kwargs</code> <p>Per-field shorthands forwarded to :class:<code>FnForm</code>.</p> <p> TYPE: <code>Any</code> DEFAULT: <code>{}</code> </p>"},{"location":"#fnform","title":"FnForm","text":"<p>               Bases: <code>Form</code></p>"},{"location":"#dash_fn_form.FnForm.call","title":"call","text":"<pre><code>call(values)\n</code></pre> <p>Validate values and call the wrapped function.</p> <p>Returns <code>(result, errors)</code>.  If validation fails, <code>result</code> is <code>None</code> and <code>errors</code> maps field names to error messages.  On success, <code>errors</code> is <code>{}</code>.</p> <p>Example::</p> <pre><code>@app.callback(\n    Output(\"result\", \"children\"),\n    *cfg.validation_outputs,\n    Input(\"apply\", \"n_clicks\"),\n    *cfg.states,\n)\ndef on_apply(_n, *values):\n    result, errors = cfg.call(values)\n    if errors:\n        return dash.no_update, *cfg.invalid_outputs(errors)\n    return str(result), *cfg.invalid_outputs({})\n</code></pre>"},{"location":"#dash_fn_form.FnForm.call_named","title":"call_named","text":"<pre><code>call_named(**raw)\n</code></pre> <p>Like :meth:<code>call</code> but accepts field values as keyword arguments.</p> <p>Designed for use with :attr:<code>named_states</code> and Dash's flexible <code>inputs=dict(...)</code> callback signature, where field values arrive as named kwargs rather than a positional tuple::</p> <pre><code>@app.callback(\n    Output(\"result\", \"children\"),\n    inputs=dict(n=Input(\"apply\", \"n_clicks\"), **cfg.named_states),\n)\ndef on_apply(n, **values):\n    result, errors = cfg.call_named(**values)\n    return str(result) if not errors else str(errors)\n</code></pre>"},{"location":"#field","title":"Field","text":"<p>Per-field configuration for :class:<code>~dash_interact.FnForm</code>.</p> <p>Can be supplied in three ways (highest priority wins):</p> <ol> <li>In-signature via <code>Annotated[T, Field(...)]</code> \u2014 for functions    you own.</li> <li>External via keyword argument on    :class:<code>~dash_interact.FnForm</code> \u2014 for functions you don't own.</li> <li>Type-level <code>_styles</code> / <code>_class_names</code> dicts on    :class:<code>~dash_interact.FnForm</code> fill in any visual properties    not set by the above.</li> </ol> <p>A :class:<code>FieldHook</code> instance may also be passed directly as a kwarg and is treated as <code>Field(hook=hook)</code>.</p>"},{"location":"#dash_fn_form.Field.class_name","title":"class_name  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>class_name = ''\n</code></pre> <p>CSS class applied to the component.</p>"},{"location":"#dash_fn_form.Field.col_span","title":"col_span  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>col_span = 1\n</code></pre> <p>Column span in a multi-column grid (see <code>_cols</code> on :func:<code>build_config</code>).</p>"},{"location":"#dash_fn_form.Field.component","title":"component  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>component = None\n</code></pre> <p>Replace the auto-generated component entirely. <code>id</code> is set internally.</p>"},{"location":"#dash_fn_form.Field.component_prop","title":"component_prop  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>component_prop = 'value'\n</code></pre> <p>Property to read back from a custom <code>component</code> (default: <code>\"value\"</code>).</p>"},{"location":"#dash_fn_form.Field.debounce","title":"debounce  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>debounce = None\n</code></pre> <p>Control debounce on <code>dcc.Input</code> / <code>dcc.Textarea</code> fields.</p> <ul> <li><code>None</code> (default) \u2014 use the type default (<code>True</code> for all text and   number inputs).</li> <li><code>False</code> \u2014 update on every keystroke (useful for live search/filter).</li> <li><code>True</code> \u2014 force debounce even if the type default would be <code>False</code>.</li> </ul> <p>Has no effect on <code>bool</code>, <code>date</code>, <code>datetime</code>, <code>literal</code>, or <code>enum</code> fields, which don't use debounce.</p>"},{"location":"#dash_fn_form.Field.default","title":"default  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>default = None\n</code></pre> <p>Default value for the field.</p> <p>When set, takes precedence over the callable's own parameter default. This allows reusing existing functions while overriding their defaults via <code>field_specs</code> \u2014 analogous to <code>functools.partial</code>::</p> <pre><code>capture_graph(\n    graph,\n    renderer=my_renderer,               # has title: str = \"\"\n    field_specs={\"title\": Field(default=\"Q4 Results\")},  # wins\n)\n</code></pre> <p>Also used in :class:<code>~dash_interact.Form</code> declarative subclasses::</p> <pre><code>class MyForm(Form):\n    dpi: int = Field(min=72, max=300, default=150)\n</code></pre>"},{"location":"#dash_fn_form.Field.description","title":"description  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>description = None\n</code></pre> <p>Help text rendered below the component.</p>"},{"location":"#dash_fn_form.Field.ge","title":"ge  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ge = field(default=None, repr=False)\n</code></pre> <p>Alias for <code>min</code> (greater-than-or-equal). Ignored if <code>min</code> is set.</p>"},{"location":"#dash_fn_form.Field.gt","title":"gt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>gt = field(default=None, repr=False)\n</code></pre> <p>Alias for <code>min</code> (greater-than, exclusive semantics not enforced by widget). Ignored if <code>min</code> is set.</p>"},{"location":"#dash_fn_form.Field.hook","title":"hook  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>hook = None\n</code></pre> <p>Runtime hook that derives the field's default from Dash state.</p>"},{"location":"#dash_fn_form.Field.label","title":"label  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>label = None\n</code></pre> <p>Override the auto-generated label (default: param name in title case).</p>"},{"location":"#dash_fn_form.Field.le","title":"le  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>le = field(default=None, repr=False)\n</code></pre> <p>Alias for <code>max</code> (less-than-or-equal). Ignored if <code>max</code> is set.</p>"},{"location":"#dash_fn_form.Field.lt","title":"lt  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>lt = field(default=None, repr=False)\n</code></pre> <p>Alias for <code>max</code> (less-than, exclusive semantics not enforced by widget). Ignored if <code>max</code> is set.</p>"},{"location":"#dash_fn_form.Field.max","title":"max  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max = None\n</code></pre> <p>Maximum value (inclusive). Only applied to <code>int</code> / <code>float</code> fields.</p>"},{"location":"#dash_fn_form.Field.max_length","title":"max_length  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>max_length = None\n</code></pre> <p>Maximum length. For <code>str</code> / <code>path</code>: character count. For <code>list</code> / <code>tuple</code>: item count.</p>"},{"location":"#dash_fn_form.Field.min","title":"min  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>min = None\n</code></pre> <p>Minimum value (inclusive). Only applied to <code>int</code> / <code>float</code> fields.</p>"},{"location":"#dash_fn_form.Field.min_length","title":"min_length  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>min_length = None\n</code></pre> <p>Minimum length. For <code>str</code> / <code>path</code>: character count. For <code>list</code> / <code>tuple</code>: item count.</p>"},{"location":"#dash_fn_form.Field.multiple_of","title":"multiple_of  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>multiple_of = field(default=None, repr=False)\n</code></pre> <p>Alias for <code>step</code>. Ignored if <code>step</code> is set.</p>"},{"location":"#dash_fn_form.Field.pattern","title":"pattern  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>pattern = None\n</code></pre> <p>Regex the value must fully match. Only applied to <code>str</code> / <code>path</code> fields.</p>"},{"location":"#dash_fn_form.Field.persist","title":"persist  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>persist = False\n</code></pre> <p>Persist the field value across page reloads using the browser's session storage.</p> <p>When <code>True</code>, the generated component is rendered with <code>persistence=True, persistence_type=\"session\"</code> so the browser remembers the last value even after a full page refresh.  Set to <code>False</code> (default) to keep the component stateless (always loads with the default value).</p> <p>Equivalent to wrapping every field manually in a <code>dcc.Store</code> \u2014 useful for sliders or dropdowns whose position the user adjusts frequently.</p>"},{"location":"#dash_fn_form.Field.step","title":"step  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>step = None\n</code></pre> <p>Step size. Only applied to <code>int</code> / <code>float</code> fields.</p>"},{"location":"#dash_fn_form.Field.style","title":"style  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>style = None\n</code></pre> <p>CSS dict applied to the component (not the wrapper div).</p>"},{"location":"#dash_fn_form.Field.validator","title":"validator  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>validator = None\n</code></pre> <p>Custom validator called with the coerced value after type checking.</p> <p>Return a human-readable error string on failure, <code>None</code> on success.</p> <p>Example::</p> <pre><code>Field(validator=lambda v: \"Must be positive\" if v &lt;= 0 else None)\nField(validator=lambda v: None if \"@\" in v else \"Not a valid email\")\n</code></pre> <p>Can also be supplied as a bare callable in <code>Annotated</code> metadata::</p> <pre><code>def positive(v): return \"Must be &gt; 0\" if v &lt;= 0 else None\n\ndef fn(dpi: Annotated[int, positive] = 150): ...\n</code></pre>"},{"location":"#dash_fn_form.Field.visible","title":"visible  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>visible = None\n</code></pre> <p>Conditional visibility rule: <code>(\"other_field\", \"==\", value)</code>.</p>"},{"location":"#dash_fn_form.Field.widget","title":"widget  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>widget = None\n</code></pre> <p>Select an alternative widget for supported types.</p> <ul> <li><code>\"slider\"</code> \u2014 render a <code>dcc.Slider</code> (or <code>dmc.Slider</code>) instead of a   number input.  Requires <code>min</code> and <code>max</code> to be set.</li> </ul>"},{"location":"#fieldhook","title":"FieldHook","text":"<p>Base class for field hooks.</p> <p>Subclass to define fields whose default value and/or submitted value are derived from runtime Dash state rather than a static default.</p> <p>Override :meth:<code>required_states</code> to declare which Dash <code>State</code> objects your hook needs. Their values are passed positionally to :meth:<code>get_default</code> and :meth:<code>transform</code>.</p>"},{"location":"#dash_fn_form.FieldHook.get_default","title":"get_default","text":"<pre><code>get_default(*state_values)\n</code></pre> <p>Compute the initial field value from resolved state values.</p>"},{"location":"#dash_fn_form.FieldHook.required_states","title":"required_states","text":"<pre><code>required_states()\n</code></pre> <p>Dash <code>State</code> objects this hook needs at runtime.</p>"},{"location":"#dash_fn_form.FieldHook.transform","title":"transform","text":"<pre><code>transform(value, *state_values)\n</code></pre> <p>Transform the user-submitted value before it reaches the renderer.</p>"},{"location":"#fromcomponent","title":"FromComponent","text":"<p>               Bases: <code>FieldHook</code></p> <p>Read a component property as the field default.</p> PARAMETER DESCRIPTION <code>component</code> <p>Any Dash component with an <code>.id</code> attribute.</p> <p> TYPE: <code>Any</code> </p> <code>prop</code> <p>The component property to read (e.g. <code>\"value\"</code>, <code>\"figure\"</code>).</p> <p> TYPE: <code>str</code> </p>"},{"location":"#register_renderer","title":"register_renderer","text":"<p>Register a custom renderer for a Python type.</p> <p>The renderer is called with the return value of the function whenever <code>interact()</code> produces a result of the registered type (or a subclass). This avoids passing <code>_render=</code> on every <code>interact()</code> call.</p> <p>Analogous to matplotlib's unit converter registry \u2014 register once at app startup, applies everywhere.</p> <p>Later registrations for the same type overwrite earlier ones.  For subclass disambiguation, register the more-specific type after the base type; the registry is checked in insertion order and the first <code>isinstance</code> match wins.</p> <p>Example::</p> <pre><code>import pandas as pd\nfrom dash import dash_table\nfrom dash_fn_form import register_renderer\n\nregister_renderer(\n    pd.DataFrame,\n    lambda df: dash_table.DataTable(\n        data=df.to_dict(\"records\"),\n        columns=[{\"name\": c, \"id\": c} for c in df.columns],\n    ),\n)\n\n# all interact() calls that return a DataFrame now use this renderer\npanel = interact(get_data)\n</code></pre>"},{"location":"#field_id","title":"field_id","text":"<p>Return the Dash component ID for a field by name.</p>"}]}